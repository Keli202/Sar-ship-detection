# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
#It may need to be replaced with an absolute path.
path: D../datasets # dataset root dir
train: images/train  # train images (relative to 'path') 128 images
val: images/test_offshore  # val images (relative to 'path') 128 images
test:  images/test_offshore

# Classes 

names:
  0: ship